spring:
  mail:
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}

otboo:
  admin:
    name: ${ADMIN_NAME}
    email: ${ADMIN_EMAIL}
    password: ${ADMIN_PASSWORD}

jwt:
  secret: ${JWT_SECRET}